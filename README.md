# chernikov.tech — personal homepage

Graphite (Blueprint) design — IBM Plex Mono/Sans, monochrome.
Static single-page site served by a stdlib Python server.

- `main` — production branch (will serve chernikov.tech root)
- staging deploys reviewed before any root-domain change
